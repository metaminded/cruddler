<%= form_for([@house, @cat, @parasite]) do |f| %>
  <% if @parasite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parasite.errors.count, "error") %> prohibited this parasite from being saved:</h2>

      <ul>
      <% @parasite.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :legs %><br />
    <%= f.number_field :legs %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
